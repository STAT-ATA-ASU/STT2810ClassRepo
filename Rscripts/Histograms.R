# Approximately normal distribution: load Galton parent child height data
library(psych)
data(galton
freq(galton$parent)
freq(galton$child)

#Bimodal distribution: Load Old Faithful dataset
data(faithful)
freq(faithful$eruptions)
hist(faithful$waiting)